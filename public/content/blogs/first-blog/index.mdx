---
title: "Why MDX is Great"
description: "Learn why MDX gives you superpowers in content + component workflows."
publishedAt: "2025-06-23"
thumbnail: "/content/blogs/first-blog/data/thumbnail.png"
isPublished: true
featured: true
---

asdf

# Title

MDX lets you use React components in markdown...

```js
const x = "Hello world";
```

showLineNumbers title="Code block title"
const x = "Hello world";

<CodeBlock language="tsx" title="Hello.tsx" showLineNumbers>
{`import MaxWidthWrapper from "@/components/max-width-wrapper";
import { Separator } from "@/components/ui/separator";
import BlogCard from "./_components/blog-card";

export default function Blogs() {
  return (
      <MaxWidthWrapper className="my-8">
        <div className="flex items-center justify-between pb-4">
          <span className="font-mono text-2xl font-bold tracking-wide">
            Blogs
          </span>
        </div>
        <Separator />
        <div className="my-4">
          <BlogCard />
        </div>
      </MaxWidthWrapper>
    );
  }`}
</CodeBlock>

Testing inline code `x = "Hello"`

**showLineNumbers**

<MarkdownTable>
  {`
| Rocket               | Height   | Payload to LEO | Stages |
|:---------------------|:--------:|:--------------:|:------:|
| SpaceX Starship      | 120 m    | 150 t          | 2      |
| NASA SLS Block 1     | 98 m     | 95 t           | 2      |
| Blue Origin New Glenn| 98 m     | 45 t           | 2      |
  `}
</MarkdownTable>

<MarkdownLatex>{`E = mc^2`}</MarkdownLatex>

<MarkdownLatex block={false}>{`a^2 + b^2 = c^2`}</MarkdownLatex>

<Callout variant="danger" title="Danger">
  ...
</Callout>

<MarkdownLatex>{`E = mc^2`}</MarkdownLatex>

<MarkdownLatex block={false}>{`a^2 + b^2 = c^2`}</MarkdownLatex>

<MarkdownLatex>{`\\frac{1}{\\sqrt{2\\pi\\sigma^2}} e^{ -\\frac{(x - \\mu)^2}{2\\sigma^2} }`}</MarkdownLatex>

<MarkdownLatex>{`\\sqrt{a^2 + b^2}`}</MarkdownLatex>

<MarkdownLatex>{`\\sum_{n=1}^{\\infty} \\frac{1}{n^2} = \\frac{\\pi^2}{6}`}</MarkdownLatex>

<MarkdownLatex>{`\\int_0^1 x^2 dx = \\frac{1}{3}`}</MarkdownLatex>

<MarkdownLatex>{`\\begin{bmatrix} 1 & 2 \\\\ 3 & 4 \\end{bmatrix}`}</MarkdownLatex>

<MarkdownLatex
  block={true}
>{`\\begin{pmatrix} a & b \\\\ c & d \\end{pmatrix}`}</MarkdownLatex>

<MarkdownLatex>{`
\\begin{aligned}
  f(x) &= x^2 + 3x + 2 \\\\
       &= (x + 1)(x + 2)
\\end{aligned}
`}</MarkdownLatex>

<MarkdownLatex>{`\\alpha + \\beta = \\gamma`}</MarkdownLatex>

<MarkdownLatex>{`\\nabla \\cdot \\vec{E} = \\frac{\\rho}{\\varepsilon_0}`}</MarkdownLatex>

<MarkdownLatex>{`
f(x) = \\begin{cases}
  x^2 & x \\ge 0 \\\\
  -x & x < 0
\\end{cases}
`}</MarkdownLatex>
